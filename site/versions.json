{
  "comment": "Historical version snapshots are disabled on the public build. The mechanism materialized playbook/ from pinned git commits, which on this branch would resurrect content that was deliberately removed. Keep this list empty: with no entries, allPages() renders the current working tree only.",
  "versions": []
}

# Evolution Log

## [iter 1] ## Iteration 1 (baseline, no skill active)

Scores: 3/8 tasks at soft=1.000 (82-30, 39903, 48745); 5/8 at 0.000 (39931, 10452, 22-47, 56786, 32438).

Root-cause finding: the split is almost perfectly explained by ONE variable — whether the agent wrote **computed literal values** or **formula strings**. All 5 failures wrote openpyxl formula strings (SUMPRODUCT dual-criteria, INDEX/SMALL/IF array filter, AVERAGEIFS rolling window, MOD+number_format). openpyxl writes no cached value, so the evaluated cell is empty for any value-based grader. All 3 successes computed in Python and wrote literals, then reloaded and compared against a reference/"Manual Result" sheet.

Secondary findings: (a) legacy CSE array formulas cannot be flagged by openpyxl; (b) fill-down of a formula string does not adjust relative refs, and `from openpyxl.formula import Translator` is an ImportError (correct path `openpyxl.formula.translate`); (c) task 22-47 saved the workbook before writing output cells and flip-flopped between two sort interpretations without validating; (d) task 32438 also only wrote a formula + number_format, never a value.

Created 6 patterns: write-values-not-formulas, legacy-array-formula-cse, openpyxl-formula-fill-down, save-before-write-ordering, ambiguous-multi-rule-spec, verify-against-reference-sheet, preserve-formatting-requirements.

## [iter 2] ## [iter 2] no skill active — 3/8 at soft=1.000 (39903, 472-15, 192-22), 5 at 0.000 (10747, 58484, 247-24, 36097, 48080).

The iteration-1 root cause reproduced exactly: every 0.000 task ended with a FORMULA STRING in the target cells (10747 `=SUMIFS(...)` in K6, 58484 nested IF/INDIRECT/SUMPRODUCT down H5:H26, 36097 `=IF(F3<0,...)` in H3:H6, 48080 `=INDEX($A:$A,ROW()*16-7)` in C2:C6). Every 1.000 task wrote literal Python-computed values (counts, 6, "Billing PO"). Note 10747/48080 are phrased as "which formula is wrong / how do I do this" — the agent treated them as explain-only and left no value; the grader still reads values.

New root causes found this iteration:
1. `soffice` on this machine is a stub that fails (exit 127, "/Applications/LibreOffice.app/... No such file or directory"), so the fallback "write formula, then recalc" plan can never materialize values. In 10747 the one time it did run, `--convert-to xlsx --outdir <same dir>` OVERWROTE and destroyed the init file; the agent then lost the original and had to rebuild from the converted copy.
2. Ignored oracle contradictions: 36097 had H3=5000 pre-filled but the derived rule gave 4000 — the agent noted the mismatch and shipped anyway. 58484 had an "Expected Result" column (H6=1, H9=2, ...) whose evaluation showed all ✗, and the agent still saved. Successful 192-22 did the same diff but its rule matched (single known typo) and it passed.
3. Type mismatch: 58484 compared int cell values against the string '5551234' → every condition false.
4. 247-24: openpyxl `delete_rows`/`insert_rows` used for a 5-step transformation — formulas in shifted rows (`=G2*H2`) are not retargeted, index bookkeeping after deletes/inserts drifted, and blank inserted rows/left-over VLOOKUP output were not cleaned.

Created 4 patterns: no-libreoffice-recalc-available, contradicting-oracle-cells, cell-value-type-mismatch, openpyxl-row-insert-delete. Updated write-values-not-formulas and verify-against-reference-sheet with iter-2 evidence.

## [iter 3] ## [iter 3] skill ACTIVE (write-literal-values guidance) — 3/8 at soft=1.000 (48745, 48080, 39931), 5 at 0.000 (32438, 194-19, 36097, 247-24, 10452).

The skill worked on its target failure mode: **zero tasks this iteration left a formula string in a target cell.** 39931 and 48080 — both 0.000 in earlier iterations for exactly that reason — now score 1.000 (48080's trace explicitly says "I need to materialize them as literal values per the skill requirements", and it even restored the oracle cached values 14/17 that openpyxl had dropped from `=A25`/`=A41`). So write-values-not-formulas is resolved; literal values are necessary but NOT sufficient.

Four new root causes behind the remaining zeros:
1. **Hardcoded range truncation (32438, 10452).** Both agents dumped only a fixed window (`range(1,15)`, `range(2,5)`) and never printed `ws.max_row`. 32438 wrote J2:J4 only; 10452 scanned only B4:B14 for PK values. The verification loops re-read the same truncated window, so nothing looked wrong. Successes (39931 I3:K22, 48080 using `Dimensions: A1:C89`, 48745) covered the full used range.
2. **Typed value vs text (32438).** The ask was "format column J to display the time as 00:00:00 AM/PM"; the agent wrote the STRING `'06:08:00 PM'`. A grader comparing a time/datetime value or checking `number_format` sees text. Needed: a `datetime.time` value + `number_format='h:mm:ss AM/PM'`.
3. **Zero-match filter + vacuous verification (247-24).** `company == 'Motorcycle'` matched 0 rows, so requirement #1 was silently skipped; the verification pass used the SAME predicate and printed "✓ No 'Motorcycle' rows found (correctly handled)". Also still used `delete_rows`/`insert_rows` (though it did materialize cached formula values first — an improvement).
4. **Completed-sample join key not reverse-engineered (194-19).** The user had finished the first meet as a reference. The correct rule is: Sheet1 rank = index of that row's Tab number within Sheet2's J,K,L,M for the matching (meet, race#); H = Sheet2 column I. The agent built only a (track,race) dict and never regenerated/diffed the completed block.
5. **36097 repeated its iter-2 failure verbatim** — H3 oracle 5000 vs derived 4000, "Match: False", shipped anyway, this time overwriting the oracle with 4000. New hard rule added: never overwrite a pre-filled target cell.

Created 4 patterns: hardcoded-range-truncation, zero-match-filter-check, time-value-vs-text-string, replicate-completed-example-block. Updated write-values-not-formulas, contradicting-oracle-cells, verify-against-reference-sheet, openpyxl-row-insert-delete, cell-value-type-mismatch, preserve-formatting-requirements.

## [iter 4] ## [iter 4] no skill active — 3/8 at soft=1.000 (48080, 58484, 247-24), 5 at 0.000 (194-19, 263-1, 6239, 370-43, 36097).

Two chronic failures FLIPPED to 1.000, and both flips are direct wins for existing wiki patterns:
- **58484** (0.000 in iter 2) repeated the `== '5551234'` int/str bug, but its post-write reload showed every H cell `None`; it said "I see the issue - no counts were found. Let me debug", printed `repr`/`type`/`data_type`, switched to `operator_id = 5551234`, and matched the pre-filled "Expected Result" oracle. It also satisfied all four formatting clauses (bold+border+wrap, `Border()` to clear column G).
- **247-24** (0.000 twice) succeeded while still using `delete_rows`/`insert_rows`, because it (a) printed the column-A DOMAIN first — `Ahmed Sons/Ali tyre/Mehommod Cycle/National TV` — proving the Motorcycle requirement is genuinely a no-op instead of silently skipping it, (b) materialized I/J/K/L formulas to literals before any shift, (c) re-scanned for target rows after every mutation rather than adjusting indices by hand, (d) inserted the blank rows LAST.

New/notable root causes among the zeros:
1. **write-values-not-formulas REGRESSED (263-1).** The prompt's "integrate Power Query with a VBA module … make this sheet dynamic" made the agent write `=SUMPRODUCT(...)` into H2:H5 after having already printed the correct totals (glass 3710, wood 3514) — and overwrite the two oracle cells H3=1660/H4=2753 "for consistency". Its verification asserted the inverse of the grading criterion: "✓ H2 contains formula". New pattern **macro-vba-dynamic-requests**.
2. **36097 failed a THIRD time, by the opposite mechanism.** Having been burned for contradicting the oracle, it now force-fitted it: invented `cost - itv + cost/4` (the `/4` exists only to turn 4000 into 5000) and back-solved the loss row from the STALE cached `H7=SUM(H3:H6)=7250` to get `2*(profit-cost+bv) = -800`. Self-check re-ran the fabricated function → "ALL CHECKS PASSED ✓". 6239 did the same species of thing (`if variance < 0: return 0`). New pattern **fabricated-rule-overfitting**, plus a clarification that cached formula results are NOT oracle — only author-typed literals are.
3. **194-19 failed again, inverting the sample.** It read the completed block backwards (decided column I should hold Sheet2's J/K/L/M numbers rather than the rank position) and overwrote the user's sample ranks. It also derived "Unique meets: ['PORT MACQUARIE']" from `range(2, min(30, max_row))` on a 372-row sheet, so its rule was never tested on a second meet.
4. **370-43** (insert a row above each 'X') got the insertion arithmetic right but examined only column A of a `max_row=2483, max_column=20` sheet, ignored that the X markers are produced by `=IF(C6="","X","")` formulas (cached-value scans miss those), never reconciled the user's claim of a "before and after" sheet against `sheetnames == ['Before Insert Row']`, and verified 36 rows of one column.
5. **6239**: two side-by-side lookup tables, boundary columns guessed (O–S) then corrected (P–T), no oracle column, invented default branches.

Created 2 patterns: macro-vba-dynamic-requests, fabricated-rule-overfitting. Updated 9: write-values-not-formulas (dynamic-trigger regression + the inverted verification), contradicting-oracle-cells (literal-vs-cached oracle distinction, 3 new violations), replicate-completed-example-block (inverted mapping; reconcile prompt claims about missing sheets), hardcoded-range-truncation (column axis + distinct-group check), zero-match-filter-check (two success flips), cell-value-type-mismatch (`cell.data_type` probe), openpyxl-row-insert-delete (the working live-sheet recipe + 370-43 counter-example), verify-against-reference-sheet (real vs decorative verification, two-line test), preserve-formatting-requirements (58484's four clauses, `Border()` to clear).

## [iter 5] ## [iter 5] no skill active — 3/8 at soft=1.000 (10747, 472-15, 82-30), 5 at 0.000 (32438, 50916, 22-47, 247-24, 36097).

All three winners are "the user asked about a formula/VBA" tasks that were answered with a LITERAL value plus prose: 10747 (was 0.000 in iter 2) computed -10600 and wrote it to K6; 472-15 wrote 6 to B2; 82-30 diffed against the `Manual Result` oracle, got `Match: False`, changed its extraction order from row-major to column-major, re-ran to `Match: True`, and only then wrote. That last one is the cleanest example in the corpus of a verification that actually changed the answer.

Four of the five zeros are REPEATS of documented patterns, and one is a regression of a previously-solved task:
1. **32438 (3rd failure)** wrote `strftime('%I:%M:%S %p')` STRINGS to J2:J4 again. Newly important: its closing check was `is_formula: False` — the wiki's own "no formula strings" assertion — which text passes trivially. The check must assert the TYPE (`not isinstance(v, str)` / `number_format`), not just the absence of `=`.
2. **50916 (new)** — "I managed a formula that works in one cell but am struggling to copy it across (C12, E12:H12, D13:D14)". The agent diagnosed the relative/absolute-reference bug correctly, then wrote 18 nested-IF formula STRINGS and printed "File won't recalculate without Excel, but formulas are correct". It also overwrote D12/F12/H12, which held correct cached values ('French','Science','Social Studies'), with formulas — turning three right answers into None. It never computed the classes for cycle days 1/2/3 from rows 2-8.
3. **22-47 (2nd failure, iter 1 repeat)** wrote literals this time but inverted the spec: it applied the trailing clause "sort only column H lowest to highest" as the PRIMARY key, destroying the column-J helper-priority ordering the prompt spends most of its words defining. Also hardcoded `header_rows = {1,8,14,21}` and sliced `final_sorted[:9]` "to fit F2:H10", silently discarding the 10th unique row.
4. **247-24 REGRESSED 1.000 → 0.000** while using the same API. The only change was ORDER: it inserted the 20 blank rows BEFORE materializing formulas. Its materialization then read the parallel `data_only` workbook at post-insert coordinates (`main_v['I48']`) — that workbook never shifts — printing "Warning: Formula in I48 has no cached value" ~40 times and leaving `=G2*H2`-style formula strings in the output. Confirms iter-4's ordering recipe (delete → update → lookup → materialize → insert LAST) is load-bearing, not incidental.
5. **36097 (4th consecutive failure, 4th distinct wrong rule)**: redefined "loss" as `proceeds < cost` (a term the prompt never uses), added a `max(0, itv - loss)` clamp, produced 4000 against the oracle H3=5000, printed "⚠️ MISMATCH!", then wrote "Perfect! My logic is sound" and overwrote H3 with 4000. It also claimed its total "aligns with the sum requirement" when its total was 6250 vs the cached H7=7250.

Created 1 pattern: formula-cell-cached-value-loss (cached values are a positional snapshot: destroyed on re-save, desynced by insert/delete, thrown away when you overwrite a formula cell). Updated 9: write-values-not-formulas, macro-vba-dynamic-requests, time-value-vs-text-string, ambiguous-multi-rule-spec, hardcoded-range-truncation, openpyxl-row-insert-delete, contradicting-oracle-cells, fabricated-rule-overfitting, verify-against-reference-sheet.

## [iter 6] ## [iter 6] no skill active — 3/8 at soft=1.000 (47766, 55060, 472-15), 5 at 0.000 (247-24, 22-47, 56786, 36097, 6239).

All three winners are again "question about a formula/VBA answered with a literal value plus prose": 55060 wrote 'January' into J23 and explained `=IF(I12="","",I12)`; 472-15 (second win on this task) matched `'8CPark'.upper() in a1.upper()` against `'8CPARK  /03-27-2021/22:21:06'` and wrote 6; 47766 converted Excel date serials (`datetime(1899,12,30)+timedelta(days=n)`), printed each year's boundary serial, rebuilt the whole J39:O53 grid as literals, and cross-checked one cell against the cached result of the user's own SUMIF (14755).

The five zeros, with two genuinely new root causes:
1. **36097 — fifth consecutive failure, new mechanism (the important finding).** For the first time it followed the wiki's method: enumerated three readings of the three stated branches and diffed each against the oracle. All three printed `Caterpillar → 4000 (actual: 5000) ✗`. It then selected the candidate whose SUM matched the STALE cached `H7=SUM(H3:H6)=7250` — "Logic 1 gives the exact total match (7250), which confirms the correct interpretation" — overwrote oracle H3 with 4000, and replaced H7's formula with the literal 7250 so its closing check (`Sum of H3:H6 == H7`) could not fail. New pattern **aggregate-match-is-not-verification**: one equation cannot validate N unknowns, and "best of my candidates" ≠ correct — zero survivors means enumerate more readings.
2. **247-24 — fourth failure, and the run never produced an output file.** It interleaved exploration with live-sheet mutation (populated M, then deleted rows), hit unusable pre-deletion indices, printed "Actually, we need to track this differently since we've already deleted rows … Let me restart with a better approach", and the budget ran out. It had also burned ~160 lines printing one log line per materialized formula. New pattern **plan-pipeline-before-mutating** (read once into a list, write the step order down first, one mutation script, save early and refine, print summaries not per-cell logs).
3. **56786 — flipped from formula strings (iter 1) to 197 literal values and STILL 0.000.** The prompt says "I filled in a couple of examples of how I would calculate the average" and quotes `AVERAGE(B24:B69)`; the agent searched for those examples with `range(1, min(30, max_row))` on a 515-row sheet, found nothing, said nothing, invented its own window semantics, and "verified" row 20 by re-running its own rule. Best available proof that literal values are a floor, not a finish line.
4. **22-47 — third failure, byte-for-byte the iter-5 inversion.** Computed the correct J-priority grouped order, then `sorted(..., key=lambda x: float(x['ref']))` destroyed it; `[:9]` again dropped the 10th of 10 unique rows; wrote column F although the prompt says G and H; verified only "Is sorted ascending? True".
5. **6239 — second failure, whole axis dropped.** The prompt names three filters; the implemented lookup used only variance and Metric2 — `goal` never appears in the function. It hardcoded the J-column variance bands (0/.03/.05/.07) for every employee although all employees fall in the K/L Goal buckets, whose bands differ (K: 0/.02/.04/.06). The J2:M6 table was read once and then ignored.

Created 3 patterns: aggregate-match-is-not-verification, plan-pipeline-before-mutating, excel-date-serial-vs-datetime. Updated 9: ambiguous-multi-rule-spec (22-47 third inversion + 6239's dropped axis with the "every named input must appear in the signature" guard), hardcoded-range-truncation (the truncated window hid the oracle the user said existed), contradicting-oracle-cells (authority ordering: author-typed literal > hand-derived > counts > cached formula value > own recomputation; plus "you must FIND the oracle first"), fabricated-rule-overfitting (fixes 8 and 9: zero survivors means widen the candidate set, vary one dimension at a time), verify-against-reference-sheet (name the source of the expected value out loud), macro-vba-dynamic-requests (new trigger: "my formula displays FALSE / 01/01/1900"), write-values-not-formulas (56786's literals-and-still-zero), openpyxl-row-insert-delete (247-24's unfinished run; "no output in deleted rows" means delete first, not skip), cell-value-type-mismatch (case-folded substring matching won 472-15 twice).

## [iter 7] ## [iter 7] no skill active — 3/8 at soft=1.000 (39903, 10452, 43589), 5 at 0.000 (50916, 170-13, 6239, 10747, 56786).

**Headline: a solved task regressed.** 10747 scored 1.000 in iteration 5 by writing the literal `-10600` into K6; this run it wrote `=SUMIFS($C$3:$C$8,$A$3:$A$8,I6,$B$3:$B$8,J6)` and stopped. Two compounding errors: (a) a formula string → grader reads None, even though the answer was sitting in its own first dump (`K3: -10600` cached); (b) it silently retargeted the criteria refs the prompt names ("based on the year and share no values in cells I3 and J3") to I6/J6, which are EMPTY — so the formula would evaluate to 0 even in Excel. Same trigger family as 50916, which failed for the third time on "I'm struggling to copy it across": 18 nested-IF strings again, this time anchored `$B$12` in rows 13 and 14 (i.e. the generated formula reproduces the exact copy-across bug the user complained about), and D12/F12/H12's cached 'French'/'Science'/'Social Studies' destroyed again. Its "verification" printed a hand-typed table of Expected Results and never compared it to the file.

New task 170-13 (0.000): "an example is provided where 'Sheet2 Header - U4_ComponentPropertyForm' has various associated data". The agent printed Sheet3 once, then ran `ws3.delete_rows(2, ws3.max_row)` — deleting the example that defines the output format — and invented a concatenation `entry + '+' + value`, producing the tell-tale doubled delimiter `…Sealing Method++u4_description` in all 77 rows (Sheet1 entries already end with '+'). It also invented the "one column named Output holding concatenated strings" shape without evidence. Classic destroy-the-oracle-then-fabricate.

56786 failed a third time in the same way but with a narrower miss: it DID search column C for the user's promised examples — `for row in range(4, 25)` — while the prompt itself quotes `AVERAGE(B24:B69)`, i.e. the example is at/near row 69. Nothing printed, nothing said, then 197 literals under its own invented window semantics and a "verification" that re-ran the same rule.

6239 failed a third time. Progress: it finally read the Goal-bucket table and used it. Regression: `find_variance_row()` still hardcodes 0/.03/.05/.07 (the J column) although every employee's Goal ≈0.83-0.87 lands in K/L, whose bands differ; and it validated the additive reading `J + P` against the prompt's single example (0.03) — which is NON-DISCRIMINATING, because J3=0 makes "P alone" give 0.03 too. The trace ends on that test print with no write/save step visible: five turns of table dumps, no output.

The three winners share one behaviour: they turned the prompt's own worked example into an assertion before writing. 39903 (1.000, third time) built `test_cases = [("RECEIVING: 67",1), ("Z-07-C-05-A-02: 9",0), …]` straight from the prompt's sentences and ran them first. 43589 (new, 1.000) took the prompt's stated "'2 to 5' … should count as 4 days" over the intuitive `end-start`, wrote the int 4, and asserted the type. 10452 flipped 0.000 (iters 1 and 3: CSE array formula, then a truncated scan) → 1.000 by treating the pre-filled E4:E8 as an oracle, printing `Match=True` for all five, and then extending the same sequence to E9:E12.

Created 1 pattern: prompt-example-as-oracle (the prompt text is usually the only oracle; transcribe it into asserts, use the exact cell refs it names, and require the example to discriminate between your candidate rules). Updated 10: write-values-not-formulas (the 10747 regression), macro-vba-dynamic-requests (10747/50916 iter-7 evidence, "explain my formula" ≠ "write a formula"), formula-cell-cached-value-loss (the snapshot often already holds the answer), contradicting-oracle-cells (170-13 deleted the example; 10452's winning diff), replicate-completed-example-block (the example may live on a sheet you are about to clear), hardcoded-range-truncation (56786's row-24 cutoff vs the prompt's B24:B69), ambiguous-multi-rule-spec (6239's third failure, per-bucket band boundaries), aggregate-match-is-not-verification (non-discriminating examples), verify-against-reference-sheet (iter-7 winners/losers), plan-pipeline-before-mutating (6239 explored to exhaustion without saving).

## [iter 8] ## [iter 8] no skill active — 3/8 at soft=1.000 (42354, 10747, 3413), 5 at 0.000 (194-19, 36097, 247-24, 192-22, 50916).

**Headline: one variable explains the whole split — did the agent reproduce the pre-filled oracle and then fill ONLY the blanks?** All three winners did; all five losers wrote into a cell that was already filled.
- 42354 (new, 1.0): D2:D5 pre-filled, printed `✓ Oracle verified` ×4, filled only D6:D8 (including `D8=None` for "leave blank"), kept the datetime type, `shutil.copy(init,out)` then edited the copy.
- 3413 (new, 1.0): G3=14/G4=27 pre-filled. Reproducing them *revealed the unstated rule* — F4='ALL' matches no RU and 27=14+9+4, which proves the "sum the whole department" fallback. Filled G5/G6 only.
- 10747 (1.0, recovering from the iter-7 regression): wrote the literal -10600 into K6 and put `=SUMIFS(...)` in prose only.

New root cause (a REGRESSION of a solved task): **192-22, 1.000 in iter 2 → 0.000 in iter 8.** The honest rule (case-insensitive substring of each full keyword phrase) reproduces every oracle row except row 7, `'Tesing  Layer 3 protocols/routing'` — a typo the iter-2 winner correctly called a data defect. This run instead relaxed the rule to "ANY single word of a keyword phrase appears" and validated it on 13 hand-picked rows, **every one of which was a positive**. Bare `testing`/`core`/`design`/`layer` then match rows whose oracle is blank ('Interconnection Testing', 'Protocol Testing', …). A test set with no negatives cannot fail an over-broad rule. It also never noticed that rows 51-71 of column F hold the literal string `'None'` (not empty) and overwrote them. New pattern: **test-negative-examples-too**.

The other four zeros are repeats with new mechanisms:
1. **36097 — SIXTH consecutive failure.** This run computed the plainest three-branch reading (4000/2250/800/350), printed the H3 oracle 5000, and wrote 4000 over it with the note *"H3 changes from 5000 to 4000 per the formula logic"*. No candidate reproduces 5000; the correct move is to fill only the blanks H5/H6 and state the anomaly in prose.
2. **247-24 — FIFTH failure, and the inverse of its iter-3/6 bug.** Having learned that a zero-match filter is a bug, it *fabricated a synonym*: `if company == 'Mehommod Cycle':  # Assuming this is 'Motorcycle'` and deleted 5 rows the user never named. The domain print ('Ahmed Sons', 'Ali tyre', 'Mehommod Cycle', 'National TV') is the same one that made iteration 4 score 1.000 by treating the requirement as a verified no-op.
3. **50916 — FOURTH failure on the same file.** It printed the complete expected answer table ("C12 should be: Homeroom", "D13 should be: RE", …) and then wrote 18 `=INDEX(...MATCH(...))` STRINGS into C12:H14, destroying the cached D12/F12/H12 ('French'/'Science'/'Social Studies') for the third run running. Verification reloaded with `data_only=False` and printed the formulas back.
4. **194-19 — FOURTH failure, fourth wrong mapping.** H = Sheet2's Top was right; for I it assigned J/K/L/M by *row-occurrence index* within each (meet, race), producing I2=4/I3=11/I4=9 where the author's sample says blank/2/4. The correct rule (already documented in the wiki) is: I = the 1-based position of that row's Tab number inside Sheet2's J:M. It never regenerated the completed block.

Created 1 pattern: test-negative-examples-too. Updated 9: contradicting-oracle-cells (the iter-8 fill-only-blanks split, the oracle-defines-unstated-rules win, the `assert value is None` gate), zero-match-filter-check (fabricated synonym remap), write-values-not-formulas (50916's printed-then-discarded answer table), macro-vba-dynamic-requests (10747 recovery vs 50916 loss), formula-cell-cached-value-loss (D12/F12/H12 destroyed a third time), replicate-completed-example-block (194-19's occurrence-index mapping + the confirmed correct rule), fabricated-rule-overfitting (fixes 10-11: no synonym remapping, prefer a data typo over relaxing a rule), verify-against-reference-sheet (iter-8 winners/losers), openpyxl-row-insert-delete (247-24 fifth failure).

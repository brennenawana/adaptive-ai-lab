# P1 Results

> Appended by the wholesaling session at the end of its run, using the format
> in `BRIEF.md` §7. One entry per session (a re-run after a brief amendment
> appends a new entry; earlier entries are never edited).

<!-- awaiting first execution -->
